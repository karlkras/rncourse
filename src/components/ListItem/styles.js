import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    listItem: {
        width: "100%",
        padding: 10,
        backgroundColor: '#eee',
        margin: 5
    }
});

export default styles;
